<%= form_with(model: job_application, local: true, url: job_job_applications_path) do |form| %>
  <% if job_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_application.errors.count, "error") %> prohibited this job_application from being saved:</h2>

      <ul>
      <% job_application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    Employer: <%= @job.employer.name %></br>
    Job Title: <%= @job.title %></br>
    Job Description: <%= @job.description %></br>
  </p>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
